*Configuração da conta local do GIT:
git config --global user.name anagiovanni
git config --global user.email anagiovanni66@gmail.com

*Criar um projeto react nature com expo
npx create-expo-app SilvinhaHealthApp

*Executar o projeto 
npx expo start --localhost --android
npm i

*Restaurar projeto após clonar
y

*Para instalar o expo router
npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar

*Alterar o package json
expo-router/entry